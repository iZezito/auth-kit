services:
  web:
    build:
      context: ../..
      dockerfile: apps/web/Dockerfile
      args:
        VITE_BASE_URL: ${VITE_BASE_URL:-http://localhost:3000}
    ports:
      - "80:80"
    restart: unless-stopped
